Run `scripts/subset_font.sh` after dropping `JetBrainsMono-Regular.ttf`
here to populate this folder with `ramp.woff2` / `headings.woff2` /
`basic-latin.woff2` and their base64 (`*.b64`) counterparts, plus
`OFL.txt` from the JetBrains Mono release. Not vendored by default to
keep the repo small.
